# System files
*.log
.DS_Store

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Webstorm
.idea

# Python
.venv
venv/

# Nuxt dev/build outputs
.output
.nuxt
*.log*
.nitro
.cache
.env
.env.*         # ✅ Ignore all .env files like .env.dev, .env.local, etc.
dist

# Node dependencies
node_modules

# Vue/Nuxt
frontend/.data
# i18n fix
vue-i18n.d.ts

# Yarn
frontend/.pnp.*
frontend/.yarn/*
frontend/!.yarn/patches
frontend/!.yarn/plugins
frontend/!.yarn/releases
frontend/!.yarn/sdks
frontend/!.yarn/versions

# 🚫 Lock files (choose only one depending on your package manager)
# Ignore NPM lockfile if using Yarn
package-lock.json
# OR ignore Yarn lockfile if using NPM
# yarn.lock

# Optional: Ignore backend lockfiles too if you have a separate backend
backend/package-lock.json
backend/yarn.lock
